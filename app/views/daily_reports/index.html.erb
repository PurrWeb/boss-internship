<%= react_component(
      "DailyReportsApp",
      {
        date: UIRotaDate.format(date),
        venueId: venue.id,
        venueName: venue.name,
        weekStartDate: UIRotaDate.format(RotaWeek.new(date).start_date),
        dailyReport: daily_report && Api::V1::DailyReportSerializer.new(daily_report).as_json
      }
    ) %>