<div>
  <% if can?(:destroy, fruit_order) %>
    <div class="pull-right">
      <%= link_to(
            fruit_order_path(fruit_order),
            class: 'boss2-button boss2-button_role_delete',
            method: :delete
          ) do %>
          Delete Fruit Order
        <% end %>
    </div>
  <% end %>

  <h1>Edit Fruit Order</h1>
</div>

<%= render(partial: 'shared/fruit_order_form', locals: {
      fruit_order: fruit_order,
      url: fruit_order_path(fruit_order),
      action_text: 'Update'
    }) %>
