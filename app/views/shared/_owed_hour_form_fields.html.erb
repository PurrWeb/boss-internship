<%= form.text_field(
    :date,
    value: form.object.date && UIRotaDate.format(form.object.date),
    label: 'Week',
    class: 'date-picker'
  ) %>
<%= form.number_field(:hours, min: 0, max: 119) %>
<%= form.number_field(:minutes, min: 0, max: 59) %>
<%= form.text_area :note %>
