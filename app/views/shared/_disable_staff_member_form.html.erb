<%= form_for(
      form,
      method: :delete,
      url: url
    ) do |form| %>
    <%= form.check_box :never_rehire, label: 'Do not rehire this person (Give reason below)' %>
    <%= form.text_area :disable_reason, label: 'Reason for disabling' %>

    <%= form.submit('Disable') %>
<% end %>
