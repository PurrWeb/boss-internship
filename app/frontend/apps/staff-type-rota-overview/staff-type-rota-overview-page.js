import React, { Component } from "react"

export default class RotaOverviewPage extends Component {
    static propTypes = {
        rotaDetailsObjects: React.PropTypes.array.isRequired
    }
    render() {
        return <div>page</div>
    }
}