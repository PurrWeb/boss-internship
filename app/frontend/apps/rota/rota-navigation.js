import React from "react"

export default class RotaNavigation extends React.Component {
    static PropTypes = {
        dateOfRate: React.PropTypes.instanceOf(Date)
    }
    render(){
        return <div>
            NAV
        </div>
    }
}