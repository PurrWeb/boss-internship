import {STEPPING_BACK_REGISTRATION} from '../constants/action-names';
import {createSimpleAction} from '../helpers/actions';

const steppingBackRegistration = createSimpleAction(STEPPING_BACK_REGISTRATION);

export default steppingBackRegistration;
