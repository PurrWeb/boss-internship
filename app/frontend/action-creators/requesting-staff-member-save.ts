import {REQUESTING_STAFF_MEMBER_SAVE} from '../constants/action-names';
import {createSimpleAction} from '../helpers/actions';

const requestingStaffMemberSave = createSimpleAction(REQUESTING_STAFF_MEMBER_SAVE);

export default requestingStaffMemberSave;
